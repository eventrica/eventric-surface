name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  call_shared:
    uses: eventrica/.github/.github/workflows/continuous_integration.yml@main
    secrets: inherit